/**
 * LeadListPage
 * Main page: paginated lead table with filters
 * Implemented in Phase 2.3
 */
import { LeadListPage as LeadListContent } from '@/components/leads'

export default function LeadListPage() {
  return <LeadListContent />
}